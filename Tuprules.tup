CC = g++

CCFLAGS += -pthread
CCFLAGS += -std=c++11
CCFLAGS += -I/usr/local/include -I/usr/include/python2.7 -I/usr/include
CCFLAGS += -Wall

ifdef RELEASE
  CCFLAGS += -O3
endif

ifdef DEBUG
  CCFLAGS += -pg
  CLFLAGS += -pg
endif

CLFLAGS += -L/usr/local/lib -L/usr/lib -L/usr/lib/python2.7/config
CLFLAGS += -lboost_python-py27 -lpython2.7
CLFLAGS += -pthread
CLFLAGS += -std=c++11
